export default class Base64ToFile {
    static fileExtension(fileName: string): string;
    private view;
    private saveLink;
    private canUseSaveLink;
    private forceSaveableType;
    private blob;
    private name;
    constructor(base64: string, name: string);
    save(): void;
    private click;
    private autoBom;
    private base64toBlob;
    private extensionToMime;
}
